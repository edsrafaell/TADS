#include <stdio.h>

int main() {
    int num = 0;

    while (num < 21) {
        if (num % 2 == 1) {
            printf("%d\nO numero e impar ", num);
        } else {

        }
        num = num + 1;
    }

    return 0;
}
